const db = require("../db.js")

function newIssuer (req , res) {
    const {firm_name, sub_heading, logo, address, phone, email, pan, upi_qr, signature_image, invoice_prefix, invoice_counter, quotation_prefix, quotation_counter } = req.body

}